#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    vector<int>v;
    int n,i,a;
    cin>>n;
    while(cin>>a) v.push_back(a);
    sort(v.begin(),v.end());
    for(i=0;i<n;i++) cout<<v[i]<<" ";
    return 0;
}
